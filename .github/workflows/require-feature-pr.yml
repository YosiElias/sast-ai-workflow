on:
  pull_request:
    # still triggers on _any_ PR event, but we’ll guard below
    branches:
      - dummy-main

jobs:
  check-feature-pr:
    runs-on: ubuntu-latest

    # ← job‑level guard: only run the job if the PR is targeting dummy‑main
    if: ${{ github.event.pull_request.base.ref == 'dummy-main' }}

    steps:
      - name: Log PR info
        run: |
          echo "✅ Running companion‑PR check for PR #${{ github.event.pull_request.number }}"
          echo "   • Base branch: ${{ github.event.pull_request.base.ref }}"
          echo "   • Head branch: ${{ github.event.pull_request.head.ref }}"

      - name: Check for matching Dummy‑AIQ PR
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const pr      = context.payload.pull_request;
            console.log(`🔍 Looking for companion PR into 'dummy-aiq' from '${pr.head.ref}'`);
            const { data: companions } = await github.rest.pulls.list({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: "all",
              base: "dummy-aiq",
              head: `${context.repo.owner}:${pr.head.ref}`,
            });
            console.log(`   • Found ${companions.length} companion PR(s)`);
            if (!companions.length) {
              core.setFailed(`❌ No companion PR into 'dummy-aiq' from branch '${pr.head.ref}'.`);
            } else {
              console.log(`✅ Companion PR #${companions[0].number} found: ${companions[0].html_url}`);
            }
